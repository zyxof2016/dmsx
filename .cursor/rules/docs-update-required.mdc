---
description: 功能改动必须同步更新相关文档（并在 PR 中可追溯）
alwaysApply: true
---

# 文档同步更新规则（强制）

当你对代码做任何**可见行为变化**或**对外契约变化**（API/配置/权限/失败语义/运维探针/CLI 输出/交互）时，必须在同一轮变更中同步更新相关文档；如果没有合适文档，就新增或扩展文档。

## 何时必须更新文档

- 新增/删除/重命名**HTTP 路由**、gRPC、WebSocket、DataChannel topic
- 改动 **认证/鉴权**、RBAC、租户边界、失败/降级语义
- 新增/修改**环境变量**、配置文件、默认值、探针（readiness/liveness）
- 行为变化会影响用户使用方式、运维方式、或排障方式（日志字段/状态暴露）
- 任何“看起来像 bugfix”的改动，但会改变边界条件或错误提示

## 需要更新到哪些文档（按影响面选择）

- `docs/CHECKLIST.md`：更新对应条目的状态与一句话说明（至少包含：影响范围、验证方式/测试）
- `docs/API.md`、`openapi/*.yaml`：路由、请求/响应、状态码、鉴权要求
- `docs/SECURITY.md`：认证/权限/密钥/威胁模型相关变化
- `deploy/*`：探针、部署参数、环境变量示例
- `README.md`：开发/运行/验证命令、最小可复现路径

## 提交前自检（必须做到）

- 文档里能明确回答：**改了什么、怎么用、怎么验证、失败时怎么看**
- 文档中的环境变量名、路径、端点与代码一致
- 有测试或可复现命令支撑文档声明（至少写入 `docs/CHECKLIST.md` 的验证记录或说明）

